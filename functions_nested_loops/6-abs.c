#include "main.h"

/**
 * _abs - starting point
 * @n: input number
 *
 * Description: computes absolute value
 * Return: absolute value
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
