#include "main.h"

/**
 * add - starting point
 * @first: first number
 * @second: second number
 *
 * Description: adds 2 integers together
 * Return: results
 */

int add(int first, int second)
{
	int result;

	result = (first + second);
	return (result);
}
