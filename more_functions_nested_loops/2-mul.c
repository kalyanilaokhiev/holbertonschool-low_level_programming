#include "main.h"

/**
 * mul - starting point
 * @a: first number
 * @b: second number
 *
 * Return: multiplied solution
 */

int mul(int a, int b)
{
	int answer;

	answer = (a * b);
	return (answer);
}
